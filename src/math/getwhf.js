/**
 * Shorthand for window.innerWidth / window.innerHeight.
 * @returns {Number} The width / height fraction.
 */
export default function _getWHFraction() {
    return window.innerWidth / window.innerHeight;
}


